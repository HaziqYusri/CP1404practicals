numbers = [3, 1, 4, 1, 5, 9, 2]

# 1. Change the first element to "ten" (string)
numbers[0] = "ten"

# 2. Change the last element to 1
numbers[-1] = 1

# 3. Print all elements except the first two (using slicing)
print(numbers[2:])

print(9 in numbers)

